[user]
	name = ccoley

[push]
    default = simple
[alias]
	alias = config --get-regexp ^alias\\.
    stat = status
    unstage = reset HEAD --
    last = log -1 HEAD
[credential]
	helper = cache --timeout=43200
